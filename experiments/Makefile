all:
	@echo "Look at the makefile"

incremental_rebuild_musl_normal:
	./incremental_rebuild.py -d ../versuchung-data --mode normal -v

incremental_rebuild_musl_hash:
	./incremental_rebuild.py -d ../versuchung-data --mode clang-hash -vv
